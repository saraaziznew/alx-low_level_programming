#include <stdio.h>
/**
 * main -entry point
 * @argc: string to print
 * @argv: string to print
 * Return: 0 (success)
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
